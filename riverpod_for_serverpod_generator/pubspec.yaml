name: riverpod_for_serverpod_generator
description: Build-runner generator for Riverpod providers backed by Serverpod endpoints.
version: 2.6.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.3.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  analyzer: any
  build: any
  build_config: any
  build_runner: any
  build_runner_core: any
  code_builder: any
  dart_style: '>=3.0.0'
  glob: any
  path: any
  recase:
  yaml: any

dev_dependencies:
  lints:
  test:
